import type { OpenSlideConfig } from '@open-slide/core';

const openSlideConfig: OpenSlideConfig = {
  // Use "ko" for Korean decks so exported HTML/PDF keeps Korean-friendly
  // language metadata, font fallback, and line breaking.
  // lang: 'ko',
};

export default openSlideConfig;
